Exercise 6.31: When is it valid to return a reference? A reference to
const?

Answer:
It is valid when you don't return the address of a local variable or that it was saved in the heap.
Returning refrence to const is valid when we need a rvalue instead of lvalue.
