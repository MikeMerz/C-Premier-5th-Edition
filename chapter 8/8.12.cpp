//Exercise 8.12: Why didn’t we use in-class initializers in PersonInfo?

/*
 * Answer:
 * We didn't use in class initializers since it only has a string and a vector which both have a default constructor which
 * will be used when default constructing PersonInfo.
 */
