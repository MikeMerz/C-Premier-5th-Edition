Exercise 11.5: Explain the difference between a map and a set. When
might you use one or the other?

Answer:
Set is similar to a map in that it uses keys however in a set each key doesn't have any value.
Using set is for storing unique elements.
Using map is for storing unique elements with an additional value of somekind.