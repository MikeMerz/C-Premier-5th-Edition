Exercise 4.9: Explain the behavior of the condition in the following if:
const char *cp = "Hello World";
if (cp && *cp)

Answer:
The condition first checks if cp is a valid pointer and the object pointed to by cp can be evaluated to true. 
