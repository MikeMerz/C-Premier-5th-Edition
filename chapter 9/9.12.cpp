//Exercise 9.12: Explain the differences between the constructor that takes a
//container to copy and the constructor that takes two iterators.

/*
 * Answer:
 * The constructor that takes a container requires that both container have the same container and element type.
 * The constructor that takes two iterators requires that there exists a conversion between the elements type.
 */
